# localAI 个人助手化路线图

目标：把 localAI 从“浏览器里的本地问答框”升级成“本地优先、可记忆、可验证、可执行轻任务的个人助手”。

## 总体架构方向

主链路采用受控流程：

```text
用户输入
  -> 意图识别
  -> 结构化计划
  -> 确定性工具/知识库/记忆执行
  -> Chrome Built-in AI 负责总结、解释和润色
  -> 保存会话、引用和任务状态
```

原则：

- Chrome 内置模型负责理解和表达，不直接承担可靠计算、状态管理和长期记忆。
- 检索、引用、记忆、任务状态和文件解析由确定性代码控制。
- Prompt 必须分区：助手规则、会话上下文、个人记忆、本地知识、用户问题。
- 所有新增能力都要能在 Chrome AI API 不稳定时 graceful fallback。

## 迭代 1：Chrome AI 调用和 Prompt 基础设施

- [x] Prompt API 语言能力探测不再硬编码英文，改为按当前语言优先探测并 fallback。
- [x] offscreen 后台 Prompt 执行使用同一套语言 fallback 策略。
- [x] 抽出个人助手 Prompt 构建模块，统一组织助手规则、最近会话、本地知识和用户问题。
- [x] 为 Prompt 构建模块补充单元测试或最小样例验证。
- [x] 增加任务状态枚举：queued、checking、creating-session、downloading、running、failed、completed。

## 迭代 2：结构化个人记忆

- [x] 新增 `assistantMemoryStore`，使用 IndexedDB 保存结构化记忆。
- [x] 记忆类型先支持：用户偏好、长期事实、项目约定、任务状态。
- [x] 新增记忆 CRUD：新增、编辑、删除、清空、导出。
- [x] Prompt 构建时按相关性注入少量记忆，不把全部记忆塞进上下文。
- [x] 用户可明确要求“记住/忘记”某条信息。
- [x] 增加记忆管理 UI，用于查看、编辑、删除和导出记忆。

## 迭代 3：知识库质量升级

- [x] 支持知识库空间，例如“个人资料”“项目 A”“项目 B”。
- [x] 文档和 chunk 记录空间 ID，检索时默认只查当前空间。
- [x] 增加 chunk 预览和引用跳转。
- [x] 建立检索评测样例：query、期望命中文档、期望答案要点。
- [x] 当浏览器语义 embedding 可用时，支持重建索引。
- [x] 增加 rerank 层，避免只依赖 top 5 粗召回。

## 迭代 4：浏览器个人助手能力

- [x] 读取当前网页正文，生成摘要、问答、待办和笔记。
- [x] 支持将当前网页保存到知识库。
- [x] 为 Summarizer、Translator、Writer、Rewriter 增加输入框旁边的轻量动作。
- [x] 将摘要、翻译、写作、改写接入后台执行，避免 popup 关闭中断。

## 迭代 5：任务助手

- [x] 增加意图识别器，输出结构化 intent。
- [x] 增加计划器，输出可执行步骤和需要的工具。
- [x] 对计算、排序、格式转换等任务使用确定性程序执行。
- [x] 最终再调用 Chrome 内置模型做解释和润色。
- [x] 保存任务状态，允许用户回到上次任务继续。

## 当前下一步

迭代 5 已完成。下一步进入迭代 6：体验闭环和可靠性。

## 迭代 6：体验闭环和可靠性

- [ ] 增加任务卡片 UI，展示当前任务的 intent、plan、执行状态和使用工具。
- [ ] 在任务卡片中展示计划步骤状态：pending、ready、blocked、completed、failed。
- [ ] 为失败任务提供“继续任务”“复制错误”“查看任务详情”入口。
- [ ] 为确定性执行结果提供可折叠详情，展示原始输入、解析结果和最终结果。
- [ ] 对 popup、side panel、新标签页分别做端到端手测清单。
- [ ] 验证 popup 关闭后后台任务继续执行，并在重新打开后恢复状态。
- [ ] 增加最小 E2E 或手动验收文档，覆盖知识库引用、记忆、网页助手、文本动作、任务继续。
- [ ] 清理或替换仍使用 `document.execCommand` 的复制 fallback。

## 迭代 7：确定性工具增强

- [ ] 增强计算器：支持括号、负数、小数、百分号、求和、平均值、最小值、最大值。
- [ ] 增加日期/时间计算：日期差、几天后、工作日估算、时间区间转换。
- [ ] 增强排序器：支持文本列表、数字列表、按字段排序、中文/英文自然排序。
- [ ] 增强格式转换：JSON、CSV、Markdown 表格、列表之间双向转换。
- [ ] 增加文本统计工具：字数、词频、重复项、分组计数。
- [ ] 为每个确定性工具补充测试样例和失败用例。

## 迭代 8：记忆系统升级

- [ ] 增加“是否记住”建议：模型识别可能有长期价值的信息后等待用户确认。
- [ ] 增加记忆冲突检测：相似记忆存在时提示合并、覆盖或保留。
- [ ] 支持记忆搜索和按类型筛选。
- [ ] 支持记忆导入，和现有导出 JSON 配套。
- [ ] 增加记忆命中解释：回答中可显示使用了哪些个人记忆。
- [ ] 增加低风险偏好的半自动记忆策略，并允许用户关闭。

## 迭代 9：知识库和网页资料增强

- [ ] 支持 PDF 文档导入和文本抽取。
- [ ] 支持 DOCX 文档导入和文本抽取。
- [ ] 支持网页剪藏：保存选区、正文、标题、URL、时间和标签。
- [ ] 增加知识文件标签和搜索。
- [ ] 增加知识库空间重命名和删除。
- [ ] 增加检索评测执行脚本，自动跑 `examples/knowledge-retrieval-eval.json`。
- [ ] 引入更强的 embedding 可用性检测和索引迁移提示。

## 迭代 10：产品化和发布质量

- [ ] 整理 README：突出个人助手定位、核心能力、安装方式和隐私边界。
- [ ] 整理架构文档：同步最新 intent、plan、deterministic executor、taskState 链路。
- [ ] 增加隐私说明：哪些数据保存在 IndexedDB，哪些能力依赖 Chrome 本地模型。
- [ ] 增加错误排查文档：Chrome 版本、flags、模型下载、API 不可用、offscreen 限制。
- [ ] 增加版本发布 checklist：build、test、手测、敏感字符串扫描、Git remote 检查。
- [ ] 重新检查非公开依赖、私有 registry 和敏感字符串。
- [ ] 准备 Chrome Web Store 发布所需的图标、截图、说明和权限解释。
